<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="/">Keep Asking</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% const pages = [{
        name: 'Home',
        path: '/'
      }, {
        name: 'Events',
        path: '/events'
      }, {
        name: 'Profile',
        path: '/profile'
      }]

      if (typeof username === 'string') {
        for (let page of pages) {
          const active = (typeof pageTitle === 'string' && pageTitle === page.name)
          %>
          <li class="nav-item <%= (active) ? 'active' : ''; %>">
            <a class="nav-link" href="<%= page.path %>"><%= page.name %>
              <% if (active) { %>
                <span class="sr-only">(current)</span></a>
              <% } %>
            </a>
          </li>
        <% }
      } %>
    </ul>
    <ul class="navbar-nav ml-auto">
      <% if (typeof (username) !== 'undefined') { %>
        <li class="nav-item">
            <a class="nav-link" href="/auth/logout">Logout</a>
        </li>
      <% } else { %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Login
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="/auth/login/google">Login with Google Account</a>
            <a class="dropdown-item" href="/auth/login/princeton">Login with Princeton Account</a>
          </div>
        </li>
      <% }%>
    </ul>
  </div>
</nav>
